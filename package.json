{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start:dev": "concurrently \"cd server && node server.js\" \"cd client && webpack-dev-server\"",
    "start": "webpack --watch",
    "server": "nodemon server/server.js --ignore client",
    "build": "webpack --mode production",
    "install": "cd server && npm install && cd ../client && npm install",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
